import os

# It's recommended to use environment variables for sensitive data
ACCESS_KEY = os.environ.get('ACCESS_KEY')

# Make sure to set the 'ACCESS_KEY' environment variable in your deployment securely
